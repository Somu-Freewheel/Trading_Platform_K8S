apiVersion: v1
kind : PersistentVolumeClaim

metadata:
  name: postgres-pvc
  namespace: trading-app

spec:
    accessModes:
        - ReadWriteOnce

    resources:
        requests:
            storage: 1Gi