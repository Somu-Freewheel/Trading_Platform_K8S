apiVersion: apps/v1
kind: Deployment

metadata:
    name: postgres
    namespace: trading-app

spec:
    replicas: 1

    selector:
        matchLabels:
            app: postgres

    template:
        metadata:
            labels:
                app: postgres
        spec:
            containers:
                - name: postgres
                  image: postgres:15.3

                  env:
                    - name: POSTGRES_DB
                      value: trading_db

                    - name: POSTGRES_USER
                      value: postgres

                    - name: POSTGRES_PASSWORD
                      value: user123

                  ports:
                    - containerPort: 5432


