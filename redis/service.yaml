apiVersion: v1
kind: Service
metadata:
  name: redis-service
  namespace: trading-app

spec:
  selector:
    app: redis

  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
  #Redis is only accessed internally by the trading-app,so ClusterIP
  type: ClusterIP

  